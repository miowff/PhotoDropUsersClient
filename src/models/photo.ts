export interface PhotoExample {
  paid: boolean;
  id: string;
  albumId: number;
  url: string;
}
export interface PhotoResponse {
  paid: boolean;
  id: string;
  albumId: number;
  preview: string;
  url: string;
}
